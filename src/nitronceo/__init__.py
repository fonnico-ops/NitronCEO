"""NitronCEO — a matriz de cobrança executiva do Grupo Nitron.

O ciclo é sempre o mesmo, e é isso que diferencia esta ferramenta de um
dashboard:

    medir -> julgar -> atribuir -> cobrar -> escalar -> registrar

Um KPI que não chega até `cobrar` não pertence a este pacote.
"""

__version__ = "0.1.0"
